import React from 'react';
import "../styles/Scrollbar.css"

const Scrollbar = () => {
    return (
        <div className="Scrollbar">
            <div className='scrollbar__item' />
        </div>
    );
}

export default Scrollbar;

